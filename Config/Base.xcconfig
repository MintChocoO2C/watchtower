// Watchtower 공용 빌드 설정 (프로젝트 수준에서 참조)
//
// 개인 서명 정보(DEVELOPMENT_TEAM 등)는 저장소에 올리지 않는다.
// 같은 폴더의 Local.xcconfig(.gitignore 대상)에 두면 여기서 선택적으로 포함한다.
// 파일이 없어도 빌드는 되며, 그 경우 Xcode의 Signing & Capabilities에서 팀을 고르면 된다.
// 템플릿: Local.xcconfig.example

#include? "Local.xcconfig"
