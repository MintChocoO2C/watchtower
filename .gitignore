# macOS
.DS_Store

# Claude Code (worktrees, session state) — 단, 프로젝트 스킬(.claude/skills)은 저장소에 포함
.claude/*
!.claude/skills/

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/

# 개인 서명 설정 (팀 ID) — 공개 저장소에 올리지 않는다
Config/Local.xcconfig
